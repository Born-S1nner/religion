<p id="notice"><%= notice %></p>

<div class="jumbotron bg-light">
  <h1 class="display-4"><%= @peer.username %></h1>
  <p class="lead"><%= @peer.email %></p>
  <p class="lead"><%= @peer.religion %></p>
  <hr class="my-4">
  <h3>Messages</h3>
  <ul>
    
  </ul>
  <p class="lead">
    <%= link_to 'Add to your Friendslist', new_peer_path, class:"btn btn-primary text-light" %>
  </p>

<%= link_to 'Edit', edit_peer_path(@peer) %> |
<%= link_to 'Destroy', @peer, method: :delete, data: { confirm: 'Are you sure?' } %> |
<%= link_to 'Back', peers_path %>

</div>